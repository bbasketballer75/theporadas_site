#!/usr/bin/env node
// Simple healthcheck: sends a readiness probe to a running MCP server process via stdin/stdout pattern.
// For now this script exits 0 immediately (placeholder). Future: connect to a socket/pipe or inspect logs.
process.exit(0);
