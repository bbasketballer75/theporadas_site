# Raw media originals (keep structure, ignore heavy sources)
media/raw/*
!media/raw/.gitkeep

# Generated encoded variants can be large; keep only ladder finals if explicitly added
media/encoded/*.tmp
media/encoded/**/*.tmp
*.log
*.tmp
*.DS_Store

.env*
.flaskenv*
!.env.project
!.env.vault
node_modules

# Generated coverage reports
coverage/

# Local ESLint cache
.eslintcache

# Build output
dist/

# Lighthouse generated artifacts
lighthouse-report.report.html
lighthouse-report.report.json
lighthouse_bundle_sizes.json
.lighthouseci/
lhr-*.html
lhr-*.json
.lhr-*.html
.lhr-*.json
links.json

# Generated analysis artifacts (diffs, snapshots, axe violations)
artifacts/

# CodeQL drift transient JSON artifacts (keep markdown summaries tracked)
codeql-drift-current-alerts.json
codeql-drift-delta.json
codeql-drift-baseline.json

# Temporary comparison outputs
*.lh-cache

.vercel
# Temporarily exclude large video files until LFS is properly configured
FINAL WEDDING MEDIA/videos/final-wedding-video.mp4
FINAL WEDDING MEDIA/parents/parent_videos/*.mp4
public/media/videos/*.mp4
