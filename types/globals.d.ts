/// <reference lib="dom" />

// Vitest global types (describe, it, expect) are provided by vitest types; ensure module is loaded
// Ensure vitest types (globals) are included by the TS program.
import 'vitest';

export {};
